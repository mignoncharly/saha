{
  "_comment": "Template for deploy/.secrets.json. Copy to deploy/.secrets.json on the server and fill with REAL values. Never commit the real file. Consumed by deploy/root_setup.sh and used to populate the backend .env.",
  "SECRET_KEY": "REPLACE_ME__django_secret_key__generate_with: openssl rand -hex 32",
  "DB_PASSWORD": "REPLACE_ME__postgres_password_for_saha_user",
  "REDIS_PASSWORD": "REPLACE_ME__redis_requirepass__must_match_redis-saha.conf",
  "ADMIN_PASSWORD": "REPLACE_ME__initial_admin_user_password",
  "VAPID_PRIVATE_KEY": "REPLACE_ME__vapid_private_key__generate_with: bash scripts/generate_vapid_keys.sh",
  "VAPID_PUBLIC_KEY": "REPLACE_ME__vapid_public_key"
}
